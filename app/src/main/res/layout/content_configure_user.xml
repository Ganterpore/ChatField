<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    tools:showIn="@layout/activity_configure_user">

    <LinearLayout
        android:id="@+id/heading"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center|bottom"
        android:orientation="vertical"
        android:paddingLeft="24dp"
        android:paddingTop="56dp"
        android:paddingRight="24dp">

        <EditText
            android:id="@+id/enter_firstname"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:paddingEnd="8dp"
            android:paddingStart="8dp"
            android:hint=" Enter first name"
            android:inputType="text"
            app:layout_constraintTop_toTopOf="parent" />

        <EditText
            android:id="@+id/enter_lastname"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:paddingEnd="8dp"
            android:paddingStart="8dp"
            android:hint=" Enter last name"
            android:inputType="text"
            app:layout_constraintTop_toBottomOf="@+id/enter_firstname" />

        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/enter_bio"
            android:hint="add a bio..."
            android:inputType="textMultiLine|textCapSentences"
            android:maxLines="4"
            android:minLines="4"
            android:maxLength="256"
            android:layout_marginTop="16dp"
            android:paddingEnd="8dp"
            android:paddingStart="8dp"
            android:layout_marginBottom="16dp"
            app:layout_constraintTop_toBottomOf="@+id/enter_number"
            app:layout_constraintBottom_toTopOf="@+id/helper_check_box"/>

    </LinearLayout>

</android.support.constraint.ConstraintLayout>